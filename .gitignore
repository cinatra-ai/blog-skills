# Dependencies — no committed lockfile by design (CI installs with --no-frozen-lockfile)
node_modules/
pnpm-lock.yaml

# Build output (tsconfig outDir) + local `npm pack` tarballs
dist/
*.tsbuildinfo
*.tgz

# Environment
.env
.env.*

# Logs
*.log

# Editor / OS cruft
.DS_Store
.idea/
.vscode/
*.swp
*~

# Internal planning notes (local only)
.planning/
